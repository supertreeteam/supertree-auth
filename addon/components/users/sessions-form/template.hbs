<div class="heavy-header text-center">
  {{t 'supertreeAuth.sessions.new.title'}}
</div>
<form {{action "login" on="submit"}}>
  {{input type="email"
          autofocus="autofocus"
          name="email"
          placeholder=(t 'supertreeAuth.models.users.placeholders.email')
          value=identification}}

  {{input type="password"
          placeholder=(t 'supertreeAuth.models.users.placeholders.password')
          value=password}}

  {{forms/checkbox-field value=rememberMe
                         label=(t 'supertreeAuth.models.users.attributes.rememberMe')}}

  {{forms/submit-button class="el-button--primary el-button--block"
                        label=(t 'supertreeAuth.sessions.new.submit')
                        disabled=isPending}}
</form>
